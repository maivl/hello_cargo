{
    "name": "Rust with Node.js",
    "dockerFile": "Dockerfile",
    "customizations": {
      "vscode": {
        "settings": {
          "terminal.integrated.shell.linux": "/bin/bash",
          "extensions": [
            "rust-lang.rust",
            "ms-vscode.node-debug2"
          ]
        }
      }
    },
    "forwardPorts": [3000],
    "postCreateCommand": "npm install"
  }