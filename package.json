{
  "author": "Eric <wkf39988@gmail.com>",
  "name": "pkg-lib",
  "version": "0.1.0",
  "private": "true",
  "scripts": {
    "build:wasm": "rimraf dist && cargo build --target wasm32-unknown-unknown --release",
    "build:wasm2": "rimraf dist && wasm-pack build --out-dir dist --out-name index --target web"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "wasm-pack": "^0.12.1"
  },
  "dependencies": {}
}
